import React, { FC, ReactElement } from 'react';

const Regester: FC = function regester (): ReactElement {
  return (
    <div>
      hello, please Regester first;
    </div>
  )
}

export default Regester;
